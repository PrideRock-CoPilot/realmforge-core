{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "LoginResponse",
  "description": "Response from a successful login attempt containing session token and metadata.",
  "type": "object",
  "required": ["session_token", "actor_id", "scope", "expires_at", "audit_event_id"],
  "properties": {
    "session_token": {
      "type": "string",
      "description": "Signed session token for subsequent authenticated requests."
    },
    "actor_id": {
      "type": "string",
      "format": "uuid",
      "description": "Actor identifier matching the authenticated identity."
    },
    "scope": {
      "type": "string",
      "description": "Granted scope for this session."
    },
    "expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of session expiration."
    },
    "audit_event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Audit event identifier for this login attempt (traceability)."
    }
  },
  "additionalProperties": false
}
