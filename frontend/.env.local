# ADR-0005: Same-origin hosting — empty string means all API calls go to the same origin.
# No CORS required. Override CONTROL_API_PORT if your local Axum runs on a different port.
VITE_API_BASE_URL=
CONTROL_API_PORT=8080
